[build]
  command = "npm install && cd src/server && npm install && cd ../client && npm install && npm run build"
  publish = "src/client/dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[[redirects]]
  from   = "/api/*"
  to     = "/.netlify/functions/api"
  status = 200
  force  = true
